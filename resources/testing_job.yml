resources:
  jobs:
    Test_job:
      name: Test job
      tasks:
        - task_key: git_folder_notebook_task
          notebook_task:
            notebook_path: /Workspace/Users/juandiego.vargasrosales@databricks.com/EventHub-Ingestion/Ingestion
              from different EventHubs (1)
            source: WORKSPACE
      queue:
        enabled: true